module.exports = {
    config: {
        name: "mentionCustom",
        author: "SaGor",
        role: 0,
        shortDescription: "নির্দিষ্ট আইডি মেনশন করলে নির্দিষ্ট রিপ্লাই",
        longDescription: "",
        category: "BOT",
        guide: "{pn}"
    },

    onChat: function ({ api, event }) {

        // 🔰 এখানে ১০টি নির্দিষ্ট আইডি এবং তাদের রিপ্লাই সেট করা যাবে
        const replyMap = {

            "61574843383066": [ "ম্যাম শুধু আমার কেউ নজর দিবি না 😈" ],

            "ID_2": [ "ID 2 এর জন্য নির্দিষ্ট রিপ্লাই" ],

            "ID_3": [ "ID 3 এর জন্য নির্দিষ্ট রিপ্লাই" ],

            "ID_4": [ "ID 4 এর জন্য নির্দিষ্ট রিপ্লাই" ],

            "ID_5": [ "ID 5 এর জন্য নির্দিষ্ট রিপ্লাই" ],

            "ID_6": [ "ID 6 এর জন্য নির্দিষ্ট রিপ্লাই" ],

            "ID_7": [ "ID 7 এর জন্য নির্দিষ্ট রিপ্লাই" ],

            "ID_8": [ "ID 8 এর জন্য নির্দিষ্ট রিপ্লাই" ],

            "ID_9": [ "ID 9 এর জন্য নির্দিষ্ট রিপ্লাই" ],

            "ID_10": [ "ID 10 এর জন্য নির্দিষ্ট রিপ্লাই" ]
        };

        // মেনশন আছে কি না চেক
        if (event.mentions && Object.keys(event.mentions).length > 0) {

            const mentionedIDs = Object.keys(event.mentions);

            for (const id of mentionedIDs) {

                // আইডি ম্যাচ হলে নির্দিষ্ট রিপ্লাই পাঠাও
                if (replyMap[id]) {

                    return api.sendMessage(
                        replyMap[id][0],
                        event.threadID,
                        event.messageID
                    );
                }
            }
        }
    },

    onStart: async function () { }
};
