module.exports = {
  config: {
    name: "mentionreply",
    author: "SaGor",
    role: 0,
    shortDescription: "Specific reply for specific IDs",
    longDescription: "",
    category: "BOT",
    guide: "{pn}"
  },

  onChat: function ({ api, event }) {

    if (!event.mentions || Object.keys(event.mentions).length === 0) return;

    // ৬টা আইডি + মেসেজ
    const data = {
      "61574843383066": "ম্যাম শুধু আমার কেউ নজর দিবি না 😈",
      "61575791445819": "✨ এই মেসেজটি শুধু এই আইডির জন্য!",
      "61579730351930": "💠 এই ব্যাক্তিকে ডাকলে আমি এই মেসেজ পাঠাবো!",
      "100088836995800": "🌟 ভাই, আপনি একটি নির্দিষ্ট আইডিকে মেনশন দিয়েছেন!",
      "100012345678900": "💎 এই আইডির জন্য আলাদা মেসেজ সেট করা আছে!",
      "100098765432110": "⚡ এই আইডি মেনশন হলে সবসময় এই মেসেজটি যাবে!"
    };

    // উল্লেখিত ID গুলো
    const mentionedIDs = Object.keys(event.mentions);

    // মেনশন করা ID গুলোর একটার জন্যও যদি মেসেজ থাকে → পাঠাবে
    for (const id of mentionedIDs) {
      if (data[id]) {
        return api.sendMessage(
          data[id],
          event.threadID,
          event.messageID
        );
      }
    }
  },

  onStart: async function () {}
};
